export default class ManagerSocial{
    constructor(id, managerId, name, url, createdAt, updatedAt) {
        this.id = id;
        this.managerId = managerId;
        this.name = name;
        this.url = url;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}