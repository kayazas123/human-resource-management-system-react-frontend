export default class EmployeeSocial {
    constructor(id, employeeId, name, url, createdAt, updatedAt) {
        this.id = id;
        this.employeeId = employeeId;
        this.name = name;
        this.url = url;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}